# Profile assets

The **Generate Snake Animation** workflow writes `github-snake.svg` and `github-snake-dark.svg` here on your default branch so your profile README can load them from `raw.githubusercontent.com`.

Run the workflow once from the Actions tab if these files are missing.
